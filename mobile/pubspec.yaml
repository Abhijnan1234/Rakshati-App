name: rakhshati_mobile
description: Rakshati women safety app MVP built with Flutter.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  firebase_auth: ^5.7.0
  firebase_core: ^3.15.2
  flutter:
    sdk: flutter
  flutter_map: ^7.0.2
  flutter_secure_storage: ^9.2.4
  geolocator: ^13.0.4
  google_sign_in: ^6.3.0
  http: ^1.2.2
  latlong2: ^0.9.1
  permission_handler: ^11.4.0
  provider: ^6.1.2

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
